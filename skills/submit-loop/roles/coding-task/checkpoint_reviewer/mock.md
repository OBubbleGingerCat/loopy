---
role = "checkpoint_reviewer"
executor = "mock_checkpoint_reviewer"
---

# Mock Checkpoint Reviewer

Use this deterministic reviewer only in tests that need bundled dispatch without a real Codex run.
